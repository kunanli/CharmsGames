# CharmsGames — 開發指南

Pandora 品牌合作的三款 8-bit 像素小遊戲，共用世界觀、色盤與結算系統。

**引擎**：Godot 4.7 / GDScript / Compatibility renderer

> 這份是專案的**單一權威文件**，人與 AI 工具都讀這一份。
> `CLAUDE.md` 只是指過來的一行捷徑（Claude Code 會自動載入它）；
> Codex 之類的工具會自動載入本檔 `AGENTS.md`。**不要把內容複製成兩份**，
> 兩份規格文件一定會分歧，之後沒人知道該信哪一個。

**規格的最終權威是 `Guides/` 裡的三份 GDD**，不是這份檔案。本檔記的是
「實作上的決定」與「GDD 沒寫、或寫了但行不通、我們自己補的部分」。
凡是刻意偏離 GDD 的地方，本檔都會寫明原因。

---

## 目前狀態

| 遊戲 | 狀態 |
|---|---|
| CharmsSeeker | 做到 **M4**（月光能量石化＋擊碎暗影猫）。M5（每 20 秒加貓）未做。 |
| CharmsFishing | 四物件改版完成（2026-08：鑽石／寶珠／雲朵／小惡魔，數量翻倍、生成區由企劃調整） |
| CharmsCatch | 依 GDD 完成首版，移動手感已依試玩調整；掉落物數量翻倍（2026-09） |

**三款畫面上的所有東西都是程式畫的色塊與線條，`assets/` 是空的。**
第一次跑起來會看到粉紅色方框的露娜、紫色方框的貓 —— 那是預期的，不是壞掉。
接美術素材是 M6。

Game feel（震動／粒子／擠壓／頓格）已完成並經過試玩一輪，
但 `SUBTLE` / `ARCADE` 兩組預設**還沒選定**，目前三款都掛 ARCADE。

---

## 快速上手

1. 裝 **Godot 4.7**（純 GDScript，不需要 .NET 版）
2. Godot Project Manager → Import → 選這個 repo 的 `project.godot`
3. 按 Run（F5 也可以）
4. 一級標題（Title_ChooseGames.png 全屏）是**管理員的遊戲選擇畫面**：
   **↑ ↓** 循環選項目（MAZE → FISHING → CATCH → SETTING → MAZE）、
   **A** 確認進該款二級標題 —— 選中 SETTING 時 A 進 **SETTING 二級選單**
   （見「SETTING 管理員設定」段）；**B 鍵沒有任何功能**（原「按 B 開
   排行榜清除選單」已於 2026-09 拆除，清除統一走 SETTING）。所有選單文字
   都在 1920×1080 設計座標 (734,392) 起、651×291 的區域內。
   二級標題（背景是各款的全屏影片 `assets/title/TItleVideo/Title_*.ogv`，
   等比例縮放填滿 480×270 —— 素材是 16:9 所以正好全屏；影片沒進場時退回
   標題圖 `Title_*.jpg`）**沒有按鈕**，底部有一行閃爍的
   **PRESS ANY BUTTON TO START** 提示（亮滅二值閃爍、週期 1.2 秒亮 0.7 秒，
   起名 overlay 不畫這行）：
   按**任意鍵**（方向鍵／A／B／空格／字母鍵……都可以）→ 起名開局；
   例外有三：**Y**＝投幣（見「投幣系統」段）、**A＋B 同時按住 3 秒** → 進入
   **管理員密碼界面**
   （見下，2026-08 取代舊的 START／RANKING 大按鈕與 R／F3 入口 ——
   舊按鈕矩形需求 (60,590)+720×390、`TITLE_START_BTN`／`TITLE_RANKING_BTN`
   常量與自繪按鈕已全部拆除，美術對位不需要了）。
   玩家從二級標題起名開局（難度已取消，見「三款共通的設計規格」）。
   二級標題**沒有待機功能**（2026-09 移除原「10 秒無操作 → 播待機影片＋
   CLICK TO PLAY」整段；`GAMES` 的 `idle_video` 欄位與 `assets/title/
   IdleVideo/` 素材自此不使用，素材檔留著不刪）。
   起名界面是**疊在二級標題上的半透明 overlay**：50% 黑罩
   透出二級標題圖，上面再疊各遊戲的起名彈窗圖
   （`assets/title/Naming/Name_Charms*.png`，RGBA 只有彈窗區域不透明），
   起名是**街機虛擬鍵盤**（街機沒有鍵盤滑鼠，不能依賴系統鍵盤／軟鍵盤）：
   **↑ ↓ ← →**（搖杆）移動選擇框、**A**（或 Enter／空白）確認選字、
   **B** 刪除最後一碼、**X** 清空全部、**Y** 預留、**ESC** 取消回二級標題。
   鍵盤是 0-9／A-Z 共 36 個字元鍵＋最後一列的 **OK** 鈕與 **←** 回退鈕
   （2026-09：OK／← 與字元鍵同一套按鍵素材、普通／選中兩態、**各佔兩格
   寬**；← 功能與 B 相同）；名字只收 A-Z／0-9、最多 9 字，OK 時名字不能為空
   （空名按 OK 只有輸入框閃＋OK 抖的回饋）。
   起名屏初始**名字為空**（2026-09 起不再預填 pandora）且預設選中
   第一格（"0"）。**名字為空時按 B（或鍵盤 ← 鈕）＝中止起名**：回二級
   標題並**退回開局吃掉的那枚幣**（`CoinManager.refund_start_cost()`，
   見「投幣系統」段）；ESC 取消不退幣。
   二級標題是固定場所：**不響應 1/2/3 與 ESC**，
   唯一回一級的路徑是 **管理員密碼界面**（二級標題 **A＋B 同時按住 3 秒**
   進入）：密碼是**方向指令序列「上上下下左右左右」**（↑ ↑ ↓ ↓ ← → ← →
   共 8 位），玩家每按一個**方向鍵**就輸入一位，輸滿 8 位後按 **A** 確認 ——
   正確 → 回一級標題；錯誤 → 清空重填（B 刪除最後一位、沒輸入內容時
   按 B 等同 ESC 直接回二級）。
   遊戲中 **ESC** 與排行榜面板的 **B／ESC** 都回**該款的二級標題**
   （名字清除）

操作：

| | 移動 | 主動技能 | 備註 |
|---|---|---|---|
| Seeker | 方向鍵（**不支援 WASD**，A 被技能佔用） | **A** 啟動 5 秒石化 | 需先撿到月光能量 |
| Fishing | ← → 探看湖面 | **A**／空白／↓／Enter 放線，**B**／**X** 用月光能量 | 月光能量只在收線途中有效 |
| Catch | ← → | 無（護盾已於 2026-09 取消，B／X 沒有功能） | 長按同方向 1 秒加速至 ×2.5（A/Shift 衝刺已移除） |

**街機按鍵綁定（2026-09，`shared/arcade_input.gd` ＋ project.godot
`[input]`，鍵盤與 Xbox 手柄同時生效）**：**A**＝鍵盤 A／手柄 A、
**B**＝**鍵盤 S**／手柄 B（2026-09 起**鍵盤 B 不再有任何功能**，原 B 邏輯
全部改到 S）、投幣 **Y**＝鍵盤 Y／手柄 View/Back（button_index 4）。
上表與各段的 A／B／投幣都指街機按鍵；程式端一律走 InputMap action
（`arcade_a`／`arcade_b`／`coin_insert`）判斷，不要直接比 KEY_A／KEY_B
keycode。**方向鍵沒有整綁手柄**：管理員界面（一級清單／SETTING 二三四級）
的 ↑↓ 選擇與**管理員密碼的方向輸入**都額外吃**手柄左搖杆**（`launcher.gd`
的 `_pad_stick_nav` 與 `ui/admin_password.gd` 的 `_stick_direction`，
死區 ±0.5、邊沿觸發不連發、回中立區才能推下一次）；三款遊戲內的移動走
內建 `ui_left` 等 action（手柄十字鍵／左搖杆引擎預設已綁）；
起名屏本來就吃手柄（十字鍵／左搖杆／A B X Y）。
手柄按鈕事件不進 `_unhandled_key_input`，同時吃鍵盤＋手柄的界面
（launcher 標題層／密碼彈窗／清除選單／排行榜面板）都改用
`_unhandled_input` ＋ `ArcadeInput.pressed/released`。

局終進 **Game Over 動畫**（`ui/game_over.gd`，三款共用）：**背景保留
遊戲場景** —— launcher 暫停遊戲節點定格在最後一幀當底（不釋放、不是
二級標題圖），遊戲自己的 RESULT 壓暗遮罩照畫；動畫只有 GAME OVER／
TIME UP 文字的淡入淡出（0.35 秒淡入 → 停留至第 1 秒 → 0.3 秒淡出），
**不顯示分數／名字／排名**（那些交給排行榜面板），播完**自動進排行榜
面板** —— 沒有按鈕、不吃任何按鍵（舊版 RESTART／LEADERBOARD 兩鈕已於
2026-09 移除）。

---

## 排行榜（本地，三款共用）

一套本地排行榜系統，選單 → 輸入名字 → 玩 → 局終自動提交成績 →
**Game Over 動畫** → 排行榜面板。
儲存在 `user://leaderboard.json`（`version:1`，每款最多 1000 條，各按 `game_id`
分榜互不干擾；檔案損壞自動改名 `.bak` 以空資料重來；**新提交的記錄永不因
裁剪丟失**，刪的永遠是最差的老記錄）。

- **檔案分工**：`shared/leaderboard_record.gd`（一條記錄的欄位與序列化）、
  `shared/leaderboard_manager.gd`（儲存／排序／分頁／清除，全部 static；
  單遊戲清除走 `clear_records(game_id, ClearRule)`、跨遊戲清除走
  `clear_all_games_records(ClearRule)`，見下）、
  `shared/player_session.gd`（玩家名字的生命週期：首次進遊戲要輸入，
  重開保留，回二級清除；見下）、
  `ui/game_over.gd`（局終 Game Over 動畫：只淡入淡出 GAME OVER／TIME UP
  文字，背景是暫停定格的遊戲場景，三款共用一份）、
  `ui/leaderboard_panel.gd`（通用排行榜面板：大標題 YOUR SCORE＋前 10 名
  單欄＋底部當前玩家行，進入時逐行交錯顯現，三款共用一份，一律只讀）、
  `ui/name_input.gd`（姓名輸入屏：街機虛擬鍵盤，搖杆＋按鍵操作，見「起名」）。
- **流程控制在 launcher**：`enum Mode { MENU, GAME_TITLE, NAME_INPUT, PLAYING, GAME_OVER, LEADERBOARD, SETTING, SETTING_GAME, SETTING_CLEAR }`。
  標題層：一級畫 `assets/title/Title_ChooseGames.png` 全屏圖、另把遊戲選擇
  清單（名字）用 draw_string 疊在圖上；**二級的背景是該款的全屏影片**
  （`assets/title/TItleVideo/Title_*.ogv`，2026-08 從靜態圖改成影片當背景
  —— 路徑在 `GAMES` 的 `title_video` 欄位，launcher 用 VideoStreamPlayer
  播放、`_draw()` 手繪等比例縮放置中，素材沒進場時退回 `title_image`
  標題圖；起名 overlay 與管理員密碼彈窗底下都透出這支影片，開局／回一級
  才停）；二級標題 A＋B 長按進入的管理員密碼界面
  （`ui/admin_password.gd`）是 Modal Overlay，開著時 launcher 不處理任何按鍵。
  遊戲**只**發 `round_finished(score, duration, game_over)` 信號，不知道排行榜存在；
  launcher 組裝 LeaderboardRecord、提交、開 Game Over 動畫界面
  （**暫停遊戲節點定格畫面當背景，動畫播完進面板前才釋放**）。
  Game Over 動畫播完**自動**開排行榜面板（沒有按鈕）；
  面板 B/ESC = 回二級（清名字）。**RESTART 已移除**（2026-09，跟著
  Game Over 兩鈕一起拆）：重玩路徑＝排行榜 B/ESC 回二級 → 任意鍵 →
  重新起名。
- **排行榜版面**（2026-09 改版，需求以 1920×1080 座標給定位、程式 ÷4）：
  大標題 **YOUR SCORE**（(960, 80) 水平置中、96px），下面**前 10 名單欄**
  垂直排列（第一行視覺中心 Y≈190、行距 72px），每行三欄左對齊、60px：
  **排名 1ST./2ND./…**（X≈370）／**玩家名字**（X≈620）／**分數**（X≈1255）。
  進入面板時**逐行交錯顯現**（淡入＋從左滑入，每行錯開 0.1 秒，只播一次）。
  **畫面最底部是當前玩家的成績行**（同樣排名／名字／分數格式；
  排名是本局實際名次、可能 >10）—— 不管本局有沒有進前 10 都會顯示。
  **配色（2026-09 企劃指定）**：前十名行文字 = 白（TEXT）；大標題與
  底部當前玩家行 = 各款指定色文字＋**白邊**（8 方向整字偏移疊白字外框，
  厚度 1 邏輯 px＝1920 設計 4px）：maze＝ACCENT_MAZE #D80E85／
  fishing＝ACCENT_FISHING #123BF4／catch＝ACCENT_CATCH #A40CE8
  （色盤為此新增三色，21→24 色）。
- **新紀錄煙花（2026-09）**：本局排名 1~3 時，排行榜面板從**玩家行顯現
  的那一刻**起全屏**循環播放** `assets/AnimationScene/UI_Animate/new_record.tscn`
  （`ui/leaderboard_panel.gd` 內實例化：素材 1600×900 等比縮到 480×270
  全屏、線性濾鏡；60 幀 @20fps 自帶循環），**直到 B/ESC 關閉排行榜** ——
  面板釋放時煙花跟著一起消失。第 4 名以後不播；素材缺檔或場景結構不符
  時靜默跳過。
- **清除功能只在 SETTING 選單**（面板一律只讀、玩家端沒有清除入口）。
  原「一級標題選中某款後按 **B** 進該款清除選單」（`ui/admin_clear_menu.gd`，
  2026-08 從排行榜面板搬過來、五種規則 LAST 1 HOUR／LAST 4 HOURS／TODAY／
  BEFORE TODAY／ALL DATA 加二次確認）已於 **2026-09 拆除** —— 檔案與
  launcher 的 `_clear_modal` 接線整段移除，一級標題 B 鍵自此沒有任何功能。
  時間判定用 played_at／played_date 字串字典序
  （定寬零填充，字典序即時間序）—— 記錄本來就有時間欄位，
  不需要另加 timestamp。面板時代的跨遊戲日期清除
  （`clear_records_by_date` 與今天／昨天／前天三個 wrapper）已無 UI 入口，
  保留作為通用工具（sim 第 6 節仍驗證）。
- **SETTING 管理員設定**（2026-08 新增，一級標題選中 SETTING 按 A 進入，
  二級選單與清除選單都畫在同一塊管理員區域內、不蓋黑罩）：二級三個選項
  **CLEAR LEADERBOARD／UNLIMITED COINS／MUSIC**，↑ ↓（或手柄左搖杆上下）
  選擇（循環）、A 執行、B/ESC 回一級（選擇狀態保留）。**UNLIMITED COINS**
  按 A 切換 ON/OFF（開關型選項的 ON/OFF 畫在名字右側），狀態跨執行保存 ——
  `shared/settings.gd`（class_name Settings，`user://settings.cfg`）；
  投幣系統已實裝（2026-09，見「投幣系統」段），開局閘門統一透過
  `CoinManager.is_unlimited_coins()` 轉讀這裡的狀態。**MUSIC**（2026-09
  新增）按 A 切換 ON/OFF，**只控制 BGM、不控制音效**：AudioManager 的
  `play_bgm()` 每次讀 `Settings.is_music_on()`，關閉時一律不播並停掉
  正在播的曲子（曲名照記，各處 play_bgm 調用點不用改）；切換當下由
  `AudioManager.apply_music_setting()` 立即停／續（打開時接續播回當前
  場合的 BGM）。預設 ON。
  **CLEAR LEADERBOARD** 按 A 進**三級「選哪一款」清除選單**（2026-09 改版，
  原本是直接進規則選單跨三款一起清）：**CLEAR MAZE DATA／CLEAR FISHING DATA／
  CLEAR CATCH DATA**（選項由 launcher 從 `GAMES` 的 `menu_name` 組出，新增
  遊戲自動多一行），↑ ↓ 選擇、A 進**四級規則選單**、B/ESC 回二級。
  四級：**CLEAR TODAY／CLEAR LAST 24 HOURS／CLEAR ALL DATA**，↑ ↓ 選擇、
  A **執行**（無二次確認）、B/ESC 回三級；執行完顯示
  `<遊戲短名> SCORE DATA CLEARED` 提示並**回二級**（選擇狀態保留）。
  **執行走 `LeaderboardManager.clear_records(game_id, ClearRule)`，只清
  三級選中的那款**；規則只吃 TODAY（今天 00:00 之後）／LAST_24_HOURS
  （現在往前 24 小時）／ALL（全部）。時間判定沿用 played_at／played_date
  字串字典序，LAST_24_HOURS 是 ClearRule 末位值（不影響舊的五種規則索引）。
  跨三款一起清的 `clear_all_games_records(ClearRule)` 自此**沒有 UI 入口**，
  保留作為通用工具（sim 仍驗證）。
- **排序**：score 降冪 → played_at 升冪 → record_id 升冪（同分先玩的高，
  第三鍵兜底排序確定性）。記錄 ID 由時間戳＋引擎毫秒＋**序號**＋隨機尾碼組成
  —— 序號在同一次執行內嚴格遞增，同毫秒連續提交也不撞（300 筆連發的
  壓力測試驗證過；最初兩版生成器都曾在連發測試下撞號，見 commit 歷史）。
- **現況**：三款都已接（Fishing 先接，Seeker／Catch 同模式補上）。發信號的
  位置各不同：Fishing 在時間到的分支、Seeker 在唯一的 `_enter_result()`、
  Catch 在兩處入口共用 `_finish_round()`（有只發一次的保險，防同幀連中兩顆炸彈）。
  launcher 對沒有 `round_finished` 的遊戲仍會自動跳過接線（兜底，目前沒用到）。
- 驗證：`python tools/sim/leaderboard_sim.py`（13 節：排序／同名／同分／分頁／
  玩家定位／日期清除／管理員清除規則／跨遊戲清除／裁剪／重載／損壞恢復／名字清洗／清空）。

第一次用 Godot 開啟專案會多出一批 `*.gd.uid`（Godot 自動生成的資源 ID），
那是正常的，**請一起 commit**。目前只有 `games/seeker/` 下的幾支有 —— 其餘是在
沒有 Godot 的環境寫的，還沒生成過。

---

## 投幣系統（2026-09）

街機代幣：**Y＝投幣**、開一局吃一枚幣；管理員 SETTING 的 UNLIMITED COINS
切「無限投幣」繞過消耗。

- **檔案分工**：`shared/coin_manager.gd`（class_name CoinManager，靜態單例
  同 CurrentPlayerSession；`get_coins／add_coin／consume_coin／
  refund_start_cost／has_coin／is_unlimited_coins`。幣量只存記憶體、啟動歸
  0、不設上限 —— 投幣是實體
  行為；要跨執行保存的只有 UNLIMITED 開關，那在 Settings）；launcher 是
  唯一動幣量的人（Y 接線、開局閘門、Coin UI 畫在 `_draw_coin_ui`），
  三款小遊戲不知道投幣存在。
- **開局閘門在 `_launch()`**（排在 NOT BUILT 判斷之後，沒建置的款不白吃
  幣）：`consume_coin()` 一次做完檢查＋扣幣 —— 無限 ON 永遠放行且**不扣
  幣**；OFF 時餘額 ≥ `START_COST`（暫定 1）才扣一枚放行。放行 →
  `UI_confirm.wav` → 起名；擋下 → 左下角 Coin 圖抖動 0.4 秒＋
  `UI_Coin_None.wav`，留在二級。起名**中止**（2026-09 新增：名字為空時按
  B／鍵盤 ←，name_input 發 `aborted`）→ `refund_start_cost()` 把那枚幣
  補回（無限 ON 時閘門沒扣過幣、refund 自動不補）；**ESC 取消仍不退幣**。
- **二級標題左下角 Coin UI**（只掛 GAME_TITLE —— 起名 overlay 不畫）：
  `assets/UI/Coin.png` 原圖 1312×1199，_ready()
  一次 LANCZOS 降到 12×12（1920×1080 設計 48×48、位置 (32,1000)）再畫，
  右側 12px 狀態文字：無限 ON 顯示「∞」／OFF 顯示「餘額/需求」（如 0/1）。
  抖動位移只走繪製層、倒數歸零自動停。
- **Y 接線**：InputMap action **`coin_insert`**（project.godot `[input]`，
  實體 Y 鍵＋手柄 View/Back，2026-09 加手柄），launcher 用
  `ArcadeInput.pressed()` 讀（`_is_coin_insert`）。投幣鍵吃掉
  事件、不觸發起名；無限 ON 也照常 +1 並播 `coin_push.wav`（實體投幣的
  聲音，規格允許；關掉 ON 後先前投的幣仍在）。
- **音效**集中在 AudioManager `SFX_PATHS`：`coin_push`（投幣）／
  `ui_coin_none`（幣不夠）／`ui_confirm`（成功進起名）。

---

## 硬規則（違反這些會造成實際問題）

- **邏輯畫面固定 480×270**，一格 tile 16px，4 倍整數放大顯示。所有座標都以這個為準，不要改。
- **不要修改 `.tscn` 場景檔**。整個專案只有一個場景 `Launcher.tscn`，裡面只有一個 Node2D。所有遊戲物件都用程式碼 `add_child()` 生成。這是刻意的架構決定 —— 讓所有邏輯都在純文字的 `.gd` 檔裡，方便 AI 讀寫與 git diff。
- **格子座標一律 `Vector2i`，像素座標一律 `Vector2`**。兩者用各遊戲自己的 `cell_center()` 轉換。混用是這個專案最容易出的 bug。
- **不要引入外部套件或 addon**。
- **不要用物理碰撞（CharacterBody2D 等）做格子移動**。已經用純數學判斷實作，改用物理會造成卡牆角與抖動。
- **只用 `shared/palette.gd` 的 24 色**（原 20 色＋2026-09 企劃指定的 MOON_LIGHT 與排行榜主色三色 ACCENT_*），不自行新增中間色。唯一被允許的變化是對既有色套 alpha，例如 `Color(Palette.NIGHT, 0.82)`。需要新色請先跟企劃討論再加進色盤。
- **HUD 文字一律用英文**。全專案文字統一用 `assets/fonts/PixelFont.ttf`（x10y12pxDenkiChipHangul，10×12 像素網格）：`launcher.gd` 的 `_ready()` 把它設成 `ThemeDB.fallback_font`，所有 `draw_string` 一次生效（引擎內建預設字型已不用）。文字維持英文 —— 這支字體只含部分漢字（640 個 CJK），中英混排的像素對齊也不可靠。字體原生渲染尺寸是 12px，新增文字尺寸時優先取 12 的整數倍（12／24），非整數倍會讓像素網格對不齊。
- **畫面位移（震動、鏡頭偏移）只能走繪製層，絕對不要寫進 `position`**。`player.gd` / `cat.gd` 每幀都用 `position` 算移動與 9px 碰撞判定，位移寫進去會被修正掉、過格時被清空，還會動到判定。用 `draw_set_transform()`，有子節點的就包一層容器節點（Seeker 的 `_world`）。

---

## 目錄結構

```
res://
├── Launcher.tscn           唯一的場景（Node2D + launcher.gd）
├── launcher/launcher.gd    標題選單與流程狀態機（一級/二級標題、A＋B 長按密碼入口）
├── games/
│   ├── seeker/             CharmsSeeker：迷宮追逐（小精靈）
│   │   ├── seeker.gd       主程式與狀態機
│   │   ├── maze.gd         迷宮資料（class_name Maze）
│   │   ├── maze_tiler.gd   邏輯迷宮 → TileMap 牆體自動拼接（class_name MazeTiler）
│   │   ├── player.gd       露娜的格子移動（class_name Player）
│   │   └── cat.gd          暗影猫追逐 AI（class_name Cat）
│   ├── fishing/fishing.gd  CharmsFishing：黃金礦工式垂釣（單檔）
│   └── catch/catch.gd      CharmsCatch：接珠寶躲炸彈（單檔）
├── shared/
│   ├── palette.gd          24 色共用色盤（class_name Palette；原 20 色＋MOON_LIGHT＋ACCENT_* 三色）
│   ├── juice.gd            全畫面：震動／鏡頭偏移／視差／頓格（class_name Juice）
│   ├── fx.gd               單一物件：粒子爆散／擠壓變形（class_name Fx）
│   ├── ready_go.gd         開場 READY 動畫：實例化 Ready_go.tscn、淡入淡出、播完自行釋放（class_name ReadyGo）
│   ├── audio_manager.gd    音訊管理器（Autoload；BGM／SFX 路徑集中在 BGM_PATHS／SFX_PATHS；MUSIC 開關只影響 BGM）
│   ├── coin_manager.gd     投幣系統：餘額與開局閘門（class_name CoinManager，靜態單例）
│   └── settings.gd         SETTING 選單的設定值（UNLIMITED COINS／MUSIC，user://settings.cfg）
├── ui/
│   ├── game_over.gd        局終 Game Over 動畫（只淡入淡出文字，背景＝暫停定格的遊戲場景）
│   ├── leaderboard_panel.gd 排行榜面板（YOUR SCORE 標題＋前 10 名單欄＋底部玩家行）
│   ├── name_input.gd       起名：街機虛擬鍵盤
│   └── admin_password.gd   管理員密碼界面（二級標題 A＋B 長按進入，方向指令密碼）
├── tools/sim/              平衡模擬腳本（見該目錄 README）
├── assets/                 美術素材（標題圖／二級背景影片 TItleVideo／
│                           起名素材 Naming 已進場；IdleVideo 待機影片
│                           2026-09 起不再使用；Seeker 的
│                           S_MAP 全屏背景、S_Player 跑步動畫場景與
│                           S_Perl/S_Moon 道具貼圖 2026-08-31 進場）
└── Guides/                 GDD、美術規格書、色盤
```

啟動選單用 `load(path)` + `set_script()` 把遊戲掛到臨時節點上，所以新增遊戲不需要新增場景，只要在 `launcher.gd` 的 `GAMES` 陣列加一筆。

生成順序不能調換：`load()` → `Node2D.new()` → `set_script()` → `add_child()`，因為 `add_child()` 那一刻才會觸發遊戲的 `_ready()`。回選單時 `remove_child()` + `queue_free()` 整個節點，遊戲自己生的子節點跟著消失，所以每款遊戲**不需要各自寫清理邏輯**。

`GAMES` 裡指到還不存在的腳本不會讓選單當掉，只會顯示 COMING SOON。

---

## 怎麼驗證改動（重要，這個專案的做法比較特別）

開發環境裡沒有 Godot binary，而這三款的平衡完全靠數值堆出來。做法是
**把 GDScript 的邏輯移植成 Python 跑幾百局，用統計回答設計問題**。

```bash
python3 tools/sim/juice_sim.py
python3 tools/sim/seeker_sim.py
python3 tools/sim/fishing_sim.py
python3 tools/sim/catch_sim.py
```

**改任何數值之前先跑一次、改完再跑一次，比較數字。** 這是唯一能回答
「中位分數會落在哪」「Combo 還疊得起來嗎」的方法。細節與同步紀律見
`tools/sim/README.md` —— 腳本裡的常數是 `.gd` 的副本，改了 `.gd` 要一起改。

這套做法實際抓到過五個會出貨的 bug（清單在該 README）。

實機試玩則留給模擬答不出來的問題：會不會暈、震動夠不夠爽、頓格是打擊感還是輸入延遲。

---

## Game feel（`shared/juice.gd`）

三款共用同一顆狀態機：撞擊震動、鏡頭偏移、背景視差、命中頓格。兩組預設 `SUBTLE` / `ARCADE`，在各遊戲的 `Juice.new(...)` 換一個字就切換，**還沒實機定案**。

**鏡頭移動只有 Fishing 有，Seeker 與 Catch 不要加**（實機試玩結論）：玩家**主動**觸發的鏡頭移動（Fishing 按方向鍵探看湖面）很舒服；**自動跟隨**角色的鏡頭移動會讓人暈 —— 迷宮裡要靠牆定位、Catch 要盯落下軌跡，畫面自己在飄，眼睛沒有可以錨定的東西。撞擊震動不受影響，那是離散事件不是持續位移。

**分層模型**（三款一致，畫的順序就是這三層）：

| 層 | 內容 | 位移 |
|---|---|---|
| FAR | 星星、月亮、遠山／屋頂剪影 | `bg_offset()` |
| WORLD | 玩家、敵人、道具，**以及它們實際踩著的背景元素** | `world_offset()` |
| HUD | 分數、時間、生命、結算文字、全螢幕閃光 | 恆為 0 |

「實際踩著的」是關鍵：**水面線、水體、地面矩形、地面線都算 WORLD 不是背景**。船釘在水面線上、露娜站在地面線上 —— 讓接觸雙方共用同一個位移，脫節就變成結構上不可能。HUD 恆定不動，因為 8px 字在 4 倍放大下抖 1px 是螢幕上 4px 的跳動，看起來是 bug。

背景一律往外多畫 `Juice.OVERDRAW`（24px），震動時才不會露出沒人畫的邊。

**幾個踩過的坑**：

- 振幅用 `pow(trauma, 1.5)` 不是平方。平方在 `shake_max=6` 時會讓 trauma 0.2 只產生 0.24px、四捨五入後是 0，小碰撞完全看不見。平方原本的理由是「讓尾巴掉到門檻以下」，但 trauma 是線性衰減、本來就精準歸零，理由不成立。
- trauma **線性**衰減。指數衰減永遠到不了 0，殘留的次像素值四捨五入後會變成永久的 ±1px 抽動。
- 震動用固定 Hz 重新取樣，不是每幀重抽 —— 每幀重抽在 144Hz 螢幕上會變成高頻嗡嗡聲。
- 漂移用 `1-exp(-k*dt)`，固定係數的 lerp 收斂速度會隨 FPS 改變。
- 位移**整個合成值只四捨五入一次**，分別 round 再相加會讓兩層對不齊。
- 頓格期間整個 `_run_state` 跳過，所以 `time_left` 不會減 —— **玩家一秒都沒被偷走**（模擬確認分數膨脹 0.00%）。
- Seeker 的星點用 `FAR` 不能用 `PEARL`：`PEARL` 就是星塵珍珠本身，會變成假目標。
- 觸發值要 **≥ 0.30** 才能在兩組預設下都看得見（`juice_sim.py` 會印可見度表）。

---

## 物件層級的效果（`shared/fx.gd`）

`juice.gd` 動的是**整個畫面**，`fx.gd` 動的是**單一物件**。實機試玩證實自動移動整個畫面會暈，所以 game feel 的重心放在 fx —— 單一物件在動不會暈，資訊量卻更大（看得出是誰、被打到哪一邊）。

- **擠壓變形**：`Fx.squash(t, axis)` 回傳縮放向量，直接餵給 `draw_set_transform()`。沿撞擊軸壓扁、垂直方向撐開。用阻尼餘弦而非單純衰減 —— 單純衰減看起來像洩氣，要有一次回彈才有彈性。用在露娜撞牆／撞邊界、提籃接取彈跳。
- **粒子**：`_fx.burst(pos, n, col, speed, life, size, grav, dir, cone)`，畫在 WORLD 層才會跟著鏡頭。水中的碎屑把 `grav` 壓到 0.1~0.3 才像在水裡飄。**珍珠只給 2 顆極小的閃光** —— 一局 205 顆，再多就是整片雜訊。
- **分數滾動**：`_score_shown` 用 `move_toward` 追 `score`，速度與差距成正比，所以 +10 幾乎瞬間、+500 會滾 0.3 秒。結算畫面從 0 滾上去，提示文字等滾完才出現。
- **收尾張力**：最後 10 秒計時器每秒脈動一次、畫面四周壓一圈漸深的暗角。原本這 10 秒在畫面上完全沒有變化，時間到就突然結束。
- **Combo 光圈**（Catch）：倍率越高，露娜身上的金圈越大越亮。原本倍率只是 HUD 上一個數字，在遊戲區裡看不到。

---

## 三款共通的設計規格

- **單一關卡，60 秒**，時間到即結算，不做關卡遞進。
- **共用外框**：一級標題 → 二級標題（**無按鈕，底部一行閃爍的 PRESS ANY
  BUTTON TO START 提示，按任意鍵起名開局**；A＋B 長按 3 秒進管理員密碼界面）→ 名字（首次進遊戲）→ 遊戲 → **Game Over 動畫**（背景保留遊戲場景，只淡入淡出 GAME OVER／TIME UP 文字，不顯示分數／名字／排名；播完自動進排行榜）→ 排行榜（大標題＋前 10 名單欄＋底部當前玩家行）→ B/ESC 回二級（名字清除）。
- **起名**：一級標題沒有難度設定（EASY/HARD 已於 2026-08 移除，← → 不再
  響應）。玩家在二級標題按任意鍵後先起名再開局。起名是**疊在二級標題上的
  三層 overlay** —— launcher 在 NAME_INPUT 模式仍畫
  二級標題圖當底層；起名層先蓋 50% 黑罩（`Color(0,0,0,0.5)`，底下的二級畫面
  透得出來），再疊各遊戲的起名彈窗圖（`assets/title/Naming/Name_Charms*.png`，
  1920×1080 RGBA，只有彈窗區域不透明），功能文字在最上。**起名是街機虛擬
  鍵盤**（`ui/name_input.gd`，2026-08 從「鍵盤自輸入＋預設名」改版）：街機
  沒有鍵盤滑鼠，**只用搖杆＋按鍵**（↑ ↓ ← → 移動、A 確認／Enter 空白同義、
  B 刪除、X 清空、Y 預留、ESC 取消）—— 不依賴系統鍵盤、不彈系統軟鍵盤。
  鍵盤資料驅動（`KEY_ROWS` 陣列，字元格＋OK／DEL 兩種功能格，未來加
  SPACE／RANDOM 只加一格字串＋一個分支）；
  0-9／A-Z 共 36 個字元鍵＋最後一列的 **OK** 鈕與 **←**（DEL 格，像素
  箭頭畫的，功能＝B 刪除／名字為空時中止起名）。**OK／← 2026-09 起與
  字元鍵同一套普通／選中素材、各佔兩格寬**（6 字元＋2 寬鍵＝10 欄，與
  上面三列對齊；原「金色底、比普通鍵稍大、兩側像素愛心」已拆），名字只收
  A-Z／0-9、最多 **9 字**，OK 確認時名字不能為空（空名只有
  輸入框閃＋OK 抖的回饋，不進下一階段）。初始**名字為空、選中第一格**
  （原「預填 pandora＋預設選中 OK」已於 2026-09 移除）。按鍵素材在
  `assets/title/Naming/NamingKeyboard/`（各款 `<game>_btn.png` 普通／
  `<game>_btn_chosen.png` 選中金色，launcher 依 `game_id` 傳入）。移動是
  **邊界夾住不繞行**：第一列再上、最左列再左都停在原位，上下換列直行對齊、
  目標列較短就夾到最後一格；輸入一碼後自動前進到下一個可輸入位置（跳過
  OK 與 ←，到底繞回第一格）。
- **寶箱系統已移除**（2026-08，刻意偏離 GDD）：局終不再揭寶箱等級、不抽寶箱、沒有任何寶箱獎勵，**只計分數與排名**。原本三款各自的 `CHEST_BRONZE/SILVER/GOLD` 常數、`chest_tier()`、面板頂部的寶箱揭示行已全部拆除，launcher 也不再傳 `chest_tiers`。不要加回來；美術的寶箱圖示（art-spec 的 `ui_chest.png`）不需要出了，原 M7 的 MysteryBox 接線也一併取消。
- **狀態機模式統一**：`READY`（開場播 `assets/AnimationScene/UI_Animate/Ready_go.tscn` 的 Ready_GO 動畫，24 幀 @14FPS；`shared/ready_go.gd` 負責實例化與淡入淡出（各 0.25 秒）、播完自行釋放。READY 時長 = `ReadyGo.FADE_SECONDS + ANIM_SECONDS + 0.05` ≈ 2.0 秒，各遊戲 `READY_TIME` 直接引用這兩個常量）→ 遊玩 → `RESULT`。暫停角色用 `set_process(false)`，不要在各處加 `if` 判斷。

---

## 各遊戲玩法規格

### CharmsSeeker（迷宮追逐）

- 22×14 tile 迷宮，外框牆 + 貓窩 + 障礙長條。**地形每局隨機生成**（2026-09，
  `maze.gd` 的 `_generate_blocks()`）：中央 Logo 牆（`LOGO_SIZE` 5×1）固定，
  其餘每局重抽 —— **先抽貓窩**（`CAVE_SIZE` 2×2 建築，本身是障礙，三隻貓
  都從窩邊出生），再抽形狀一律是 **1 行或 1 列的長條**（2~4 格，隨機翻轉方向；
  不生成多行多列的塊，也不會有 1×1 —— tile 素材畫不出孤立方塊）、**一律與
  外框隔 1 格**（最外圈走道永遠淨空，外圈一圈一定鋪得到珍珠）、長條彼此
  （含 Logo 牆與貓窩）至少隔一格、四角月光／露娜出生格受保護（貓的出生格
  在貓窩四周，由長條與貓窩的間隔規則保證淨空）、總面積對齊舊固定圖的
  40 格（`GEN_*` 常數）；生成後 BFS 驗證全走道連通，64 次失敗退回 `BLOCKS`
  固定圖＋`FALLBACK_CAVE`（後備，`seeker_sim.py` 的對照基準）。
  隨機圖因長條互不相连，**MazeCorner 轉角層永遠是空的**（轉角規則留給
  手寫地圖）。`Maze.new()` 可帶正整數 seed 重現同一張圖（除錯用），
  不帶＝每局隨機。
- 約 205 顆星塵珍珠（+10）、四角月光能量（+30）、清空全場 +500 並重鋪。
- 露娜 72 px/s，Pac-Man 式移動：輸入存成 `want`，抵達格子中心才轉彎；反方向可立即掉頭。
- 三隻暗影猫共用同一套貪婪移動：排除回頭路，選「走過去離目標最近」的方向，順序優先級 上→左→下→右。死路才准回頭。差別只在目標格算法與速度：
  - **直追** 60 px/s：目標＝露娜所在格。
  - **預判** 66 px/s：目標＝露娜前方 4 格，會和直追貓自然形成夾擊。露娜靜止時退化成直追。
  - **遊蕩** 54 px/s：目標＝隨機走道格，每 2.5 秒重抽；露娜進入 6 格內就改成直追。
  - **外觀（2026-09 起）**：三隻共用 `assets/AnimationScene/S_Cat.tscn` 的
  `chase` 動畫（8 幀 @9fps，企劃指定三隻同貼圖），向右移動時水平鏡像
  （源圖朝左，`scale.x` 負號鏡像，跟露娜同一套）；石化仍是半透明藍白呼吸、
  剩 2 秒閃白（`self_modulate` 驅動，數值沿用原版）。顯示約 1.7 格
  （內容最長邊 26px；2026-09 企劃要求調小）。**三隻在畫面上不再有
  外形差異**，辨識只能靠行為 —— 舊的「三色紫剪影＋黃眼睛」說法已過期。
  每幀「內容中心對齊節點原點」的歸一化數據在 cat.gd 的 `FRAME_BOXES`
  （中心錨點，跟露娜的腳底錨點不同），畫師改圖要重測。
- **貓窩**（`S_Cat_Cave.png`，2×2 建築、每局隨機位置、本身是障礙）：三隻貓
  都從窩正下方那一格出現（`Maze.cat_spawn_cell()`），三隻靠 0 / 2.5 / 5 秒
  登場延遲先後從同一個洞口鑽出，未登場時畫成半透明；被擊碎的貓 5 秒後
  一樣從窩下重生。
- 3 條命，碰到扣 1 命（判定距離 9px），進入 `DYING` 1.2 秒後重生，
  **分數保留、剩餘時間另扣 5 秒**（2026-09 企劃；扣到 0 就在重生後立刻結算，
  懲罰可能直接終局）。
- **月光能量不是撿到就發動**（GDD 的 Xbox 協議）：撿到 +30 分並存進 HUD，最多囤 2 個，按 **A** 才啟動 5 秒石化（2026-09 企劃：8 秒改 5 秒）。石化期間貓站著不動、畫成半透明藍白，撞上去就碎；剩 2 秒時貓閃白且畫面邊緣閃爍。
- **擊碎分數**同一次石化內倍增 50→100→200→400（第五隻之後維持 400）。被擊碎的貓 5 秒後於出生格重生。
- **重生一律回到 ACTIVE，不會以石化狀態回來**（GDD 沒寫，我們定的）：三隻出生格在中央緊鄰，若石化重生就會變成「中央蹲點等 5 秒再撞一輪」，一次石化可刷 1550 分。回到 ACTIVE 後單次石化上限是 350。重生後有 1 秒半透明待命，不會無預警秒扣命。
- **撞牆會震一下＋露娜貼著牆壓扁**（`player.gd` 的 `bumped` signal，只在移動中被擋下時觸發一次）。`player.gd` 另有一個 `blocked` 旗標表示「停著頂牆」，**目前沒有接任何效果** —— 它每幀都成立，接震動會變成每秒 60 次的閃頻。
- **移動只吃方向鍵，不支援 WASD** —— A 鍵被 GDD 指定為主動技能，會跟 WASD 的 A 打架，而 GDD 本來就只寫方向鍵與左搖桿。
- **背景與跑步動畫已接美術（2026-08-31）**：`S_MAP.png`（1920×1080 出圖）÷4
  拉伸當全屏背景（`_draw_backdrop`，圖內迷宮區域對齊 `Maze.ORIGIN`×4，不變形），
  原本補視差的程式星點（`SHOW_STARS`）隨之關閉。露娜動畫用畫師交付的
  `assets/AnimationScene/S_Player.tscn`，**2026-09 改版為三段各 7 幀 @9fps
  （300×300）**：`walk` 跑步循環（貼圖已換成 S_Girl_Run_*）、`hitwall` 撞牆播
  一次（`bumped` 觸發，露娜重新起步會打斷）、`getcaught` 被抓播一次（DYING
  觸發；原本 DYING 的露娜閃爍已由動畫取代）。tscn 只當**視覺節點**（`_ready`
  實例化掛到 player 下，AnimatedSprite2D 保持 pause、由 `_process` 用場景定義
  的 fps 手動推幀，`set_process(false)` 凍結時畫面才跟著凍）；三段在 tscn 裡
  都開了循環，「播一次」由 `play_oneshot()`/`tick_anim()` 夾在最後一幀實現，
  不改美術場景；DYING 時 player 節點凍住，getcaught 的幀由 seeker 的 DYING
  分支代推。每幀的「腳底貼底＋水平居中」歸一化數據寫在 `FRAME_BOXES`
  （三段各 7 個 Rect2，畫師改圖要重測）。顯示寬度以 walk 最寬幀 = 16px
  （走廊寬度）、腳底錨點 +8，hitwall／getcaught 同縮放下約 17px。
  **縮放必須在 `_ready()` 就設好**（`_anim.scale`）—— READY 期間 `_process`
  沒在跑，只靠 `_update_anim` 設縮放的話開場會以 300×300 原尺寸畫出巨無霸露娜。
  露娜節點在 `_world` 裡**最後 add**（貓先 add），畫在三隻貓之上 ——
  被抓時貼在貓身上播的 getcaught 動畫才不會被貓蓋住。
- **場上道具與月光庫存已接美術（2026-08-31）**：星塵珍珠 `S_Perl.png`、月光
  能量 `S_Moon.png`（都是 220×220 美術圖，`ITEM_SHOW` 等比縮到 15px 在格子
  中央顯示，內容物約 11~13px，跟舊 15×15 程式圖視覺一致，不會蓋到隔壁格）。
  HUD 左下角的月光庫存由程式圓形改成 `S_Moon.png`（14px、間距 20），跟愛心
  同一套規則：有庫存全亮、空位暗色（alpha 0.22）、用掉的那顆播 1 秒「放大
  1.25 倍＋淡出」—— `_moon_fade`/`_moon_fade_slot` 鏡像 `_heart_fade` 的寫法，
  觸發點在 `_activate_moon()` 的 `moon_stock -= 1` 之後。
- **迷宮牆體已改由邏輯地圖自動鋪成 TileMap（2026-09，`maze_tiler.gd`
  已接入 `seeker.gd`）**：把「哪格是牆」的邏輯地圖（`Maze.walls` 字典或
  ASCII `#`/`.` 文字）鋪成兩層 TileMapLayer —— **MazeBase**（基礎橫／縱牆）
  ＋ **MazeCorner**（90° 轉角疊加層），TileSet 直接沿用
  `assets/seeker/Map/TileMap/tile_maze.tscn` 裡那顆（80×80 tile、單一
  atlas source 0；素材圖實為 4×5 格 320×400，只有 8 格有內容，其餘是空格）。
  邏輯格 15px、素材格 80px，所以圖層 root 的 scale = `cell_px/80 = 0.1875`、
  位置 `Maze.ORIGIN`。拼接只看牆格四鄰（**出界視為走道**）：上下皆走道 →
  橫向帶（左端／中間1/2 按座標奇偶交替／右端）；左右皆走道 → 縱向帶
  （頂部／中間／底部）；橫縱都延續的格（90° 轉角、T 形、十字、實心矩形塊的
  每一格）→ MazeBase 疊橫向 tile（右鄰是牆用左端、左鄰是牆用右端、兩側皆牆
  用中間）＋ MazeCorner 疊「縱向牆中間1」。**孤立 1×1 牆塊沒有素材可表現**
  —— `build()` 會把它收進回傳的 missing 清單，邏輯地圖要避開。碰撞照舊以
  `Maze.walls` 為準，這兩層純視覺、不產生物理。規則驗證：
  `python tools/sim/maze_tiler_sim.py`（含 tile_maze.tscn 示範地圖的回還原、
  隨機生成 300 seed 的驗證與樣本地圖列印）。**`seeker_sim.py` 尚未同步隨機
  生成** —— 它仍以固定 BLOCKS 玩整場，要跑平衡數字前先把它改成吃隨機地圖。
  **Seeker 接入方式**：`_ready()` 建圖層後鋪 `maze.walls` **減去外框格**
  （外框照舊由 S_MAP 的彩繪邊框承擔，想全 tile 化把 `inner_walls` 換成
  `maze.walls` 即可）；Logo 牆格（5×1）也**不鋪 tile** —— `S_Hinder_logo.png`
  品牌圖以原比例（寬 5 格）掛 `_logo_view` Sprite2D、用 `LOGO_CELL` 中心對位
  （centered=true），僅保留 Logo 本體，碰撞照舊是牆；貓窩格（2×2）同樣
  不鋪 tile，`S_Cat_Cave.png` 掛 `_cave_view` Sprite2D 對到窩的中心、
  放大 1.2 倍（36px）讓它明顯是一棟多格建築；
  舊的 `S_Hinder.png` 佔位拉伸已拆除。TileMap 是子節點會蓋過 `_draw`，
  所以結算壓暗／CAUGHT!／收尾暗角／石化閃邊搬進子節點 `_overlay`
  （排在 tile／Logo 之後、`_world` 之前，層級與舊版完全一致）；
  牆層與 `_world` 每幀吃同一個 `world_offset()`，震動不脫格。

### CharmsFishing（黃金礦工）

- 鉤子在船底支點 (240, 96) 以 ±75° 擺動，週期 2.4 秒，**最後 15 秒擺速 +15%**。
- **A / 空白 / 方向鍵下 / Enter**放線（GDD 指定 A），放出去不能取消。**B / X** 用月光能量，每局 3 次，只在收線途中有效：寶物 ×3 速，廢物（小惡魔）直接丟棄止損。
- **← → 探看湖面**（三款中唯一有鏡頭移動的，只做水平 —— 垂直漂移會讓水面線上下滑，看起來像船在沉）。
- **水下只有四種物件**（2026-08 改版，舊的雜魚／小珍珠／大魚／星塵珍珠／石頭／暗影猫魚全部移除）：
  - **鑽石**（`F_Diamond_s.png`）＋200 / 收線 85 / 32×32 —— 生成於**中／深層**，最重的寶物，鉤中最震；表面有**白光二值閃爍**（十字星芒，0.4 秒週期、`_draw_diamond_flash()`）。
  - **寶珠**（`F_Charm_s.png`）＋100 / 收線 85 / 32×32 —— 生成於**中層**。
  - **雲朵**（`F_Cloud_s.png`）＋10 / 收線 160 / 32×32 —— **任意層**，最輕，鉤中幾乎沒感覺。
  - **小惡魔**（`F_Imp.png`）0 分 / 收線 105 / 16×16 —— **任意層**，會橫向游動、放線時主動靠近鉤頭，上船 −3 秒。
- **生成區域**（2026-08 改版，位置由企劃直接調過）：淺／中／深 = (112,168)／(162,208)／(212,256)、全區 (112,256)。數量翻倍：每局 **4 寶珠＋6 鑽石＋8 雲朵＋4 小惡魔＝22 件**。
- **放置順序「帶最窄的先放」**：寶珠（中層帶只有 46px 高）→ 鑽石（中／深層帶大得多，放後面容錯高）→ 雲朵 → 小惡魔。反過來放寶珠會偶爾生不出來（模擬實測）。嘗試次數 40 → **500**：翻倍數量＋32px 大物件後空間很緊，40 次會偶爾短缺。
- **重量是另一回事**：重量只決定收線速度，輕→快、中→普通、重→慢、極重→極慢。鑽石與寶珠最重（85）收得最慢，雲朵最輕（160）幾乎瞬間。鉤中時的震動力道與重量成反比，所以還沒看清楚就先感覺到鉤到什麼。
- 無生命值，錯誤只以「浪費時間」懲罰。
- **族群補充機制（2026-08 改版）**：四種全部回補（同一種類、同一層位），**重生延遲 5~15 秒、隨剩餘時間由慢到快**（開局撈走的等最久 15 秒、局末 5 秒就補回來）。不加這條的話盤面總值上限只有 1680 分（6×200+4×100+8×10），手速再快也撞天花板（`fishing_sim.py` 會示範）。
- **水層不是等面積的**：鉤子可及範圍是倒三角形，越淺越窄。淺層約 13900px²、中層約 15900px²、深層約 12100px²，配額要照這個算（目前每局 4 寶珠＋6 鑽石＋8 雲朵＋4 小惡魔＝22 件，模擬過放置零失敗）。放置時一律檢查「鉤得到」，避免生出永遠撈不到的死內容；物件間先要求 **4px** 空隙、擠不下退讓成不重疊。
- **左下／右下角固定 4 顆鑽石（2026-09）**：一邊 2 顆，出生點在**畫面角落的 300×500 區域內隨機**（需求以 1920×1080 座標給定、程式 ÷4 = 邏輯 75×125，`CORNER_DIAMOND_REGION`，錨在螢幕左下／右下角），在隨機物件之前放置、`_spawn_one` 會自動避開；**這是額外加的，隨機 6 顆鑽石照舊**。區域大部分在鉤子可及錐之外（`LINE_MAX` 205），只有靠內側的一小片撈得到 —— 想整區可撈得加大 `LINE_MAX` 或縮小區域。
- **小惡魔之間沒有碰撞體積（2026-09）**：互相穿過、可以重疊。舊的兩兩推開（`IMP_MIN_SEP`）會讓反向游的兩隻互相頂住卡死，已移除——不要加回來。
- **船與露娜已換動畫場景（2026-08-31）**：`assets/fishing/F_Luna.png` 靜態貼圖
  由 `assets/AnimationScene/F_Player.tscn` 取代（AnimatedSprite2D 四段動畫：
  **Idle** 待機循環（鉤子擺動、無操作時）／**Hook** 放線循環（A 按下、線伸出
  **與收回途中**都播）／**GetPoint** 寶物上船加分播一次／**Hurt** 小惡魔上船
  扣時間播一次；一次性動畫播完自動回基礎動畫）。實例掛到預留的 **`luna_view`**
  接口（純視覺節點，position 每幀對齊 `_luna_anchor() + _juice.world_offset()`，
  只當繪製錨點、不參與玩法判定），`_draw_boat()` 偵測到 luna_view 非 null 就
  跳過舊貼圖（場景結構不符時退回）。**動畫不自己播** —— pause 後由 `_process`
  用場景定義的 fps 手動推幀（launcher 停掉遊戲節點時畫面才跟著凍）。顯示縮放
  `PLAYER_SCALE = 0.2`（與舊貼圖 300×300 畫到 60×60 同尺寸）；錨點數據在
  `P_REFS`（每段動畫取第 0 幀的 alpha>32 包圍盒，「船底貼水面線＋水平置中」，
  整段動畫共用一個 offset —— 畫師所有幀都在同一畫布、內容位置一致，
  **不做逐幀歸一化**：逐幀錨定會讓船跟著角色肢體擺動而左右滑，畫師改圖要重測）。
- **鉤頭是整張貼圖**（`assets/fishing/F_Hook.png`，100×100）：繩結點對齊線頭、
  擺動時以繩結點為軸隨 `angle` 旋轉（Godot 正角順時針、與 angle 的方向定義
  相反，所以取 `-angle`）；繩結點貼圖座標在 `HOOK_ROPE`、顯示大小 `HOOK_SIZE`，
  畫師改圖要重測這兩個常量。釣線顏色是 `LINE_COLOR`（#ff81f3），勾中瞬間
  照舊整條閃白（GDD 指定）。

### CharmsCatch（接珠寶）

- 露娜在底部左右移動 **95 px/s**，**長按同一方向 1 秒**線性加速到 **×2.5**（放開或換向就歸零重算）。人物與提籃融合成單一物件，腳踩螢幕最底（`LUNA_Y` 270），接取判定框＝`cc_person1` 貼圖大小（90×71、水平置中，見 `_catch_rect()`），掉落物中心碰到任何高度都算接到；漏接線同步在螢幕最底（`KILL_Y` 270）。
- **露娜視覺已接美術（2026-09）**：`assets/AnimationScene/C_Player.tscn`
  （AnimatedSprite2D 兩段：`Idle` 8 幀 @9fps 循環；`Hurt` 3 幀 @6fps，
  **接到炸彈扣命時播一次**再回 Idle —— 炸彈一律扣命，沒有擋下的分支）。
  實例掛到子節點 `luna_view`（z=-1、純視覺、position 只當繪製錨點不參與判定），
  `_anim.pause()` 後由 `_process` 用場景定義的 fps 手動推幀（launcher 凍結
  遊戲節點時畫面跟著凍）；擠壓變形（撞牆／接取彈跳）改寫在 `luna_view.scale`
  （節點原點＝腳底錨點，效果同舊 draw_set_transform）。錨點數據在 `P_REFS`
  （每段取第 0 幀 alpha>32 包圍盒、整段共用，畫師改圖要重測）、顯示縮放
  `PLAYER_SCALE = 0.29`（幀 450×284、內容 308×240 → 約 89×70，與判定框
  視覺一致）。**背景因此搬到 `BgView` 子節點（z=-2，fishing 同款結構）** ——
  子節點一律畫在父節點 `_draw()` 之後，背景留在父節點會蓋住玩家；遊戲本體
  `_draw` 只畫 z=0 的掉落物／粒子／HUD。場景結構不符時退回 `cc_person1`
  靜態貼圖（判定框不受影響）。
- **速度刻意偏離 GDD**（GDD 寫 60 px/s）：60 玩起來太鈍，一趟落下只夠橫移 1.4~2.9 軌，常常眼睜睜看珠寶掉在搆不到的地方。企劃試玩後決定調到 95 並加上慣性（`ACCEL` 900 / `FRICTION` 700，放開後滑約 0.14 秒 7px）。用純數學 `move_toward`，不引入物理節點。GDD 的 **A 鍵衝刺 ×1.8 已於 2026-08 拍板移除**（實機幾乎沒人用），改成上面的長按加速。
- **難度隨這幾次改動先降後升再降再升**：速度 60→95 讓接取率 79%→86%、中位分數 3400→4300；判定框融合成貼圖大小後再升到 4450／85%；移除衝刺改長按 ×1.5 後掉到 3350／79% —— 瞬發 ×1.8 換成要暖機 1 秒的 ×1.5，有效機動力大減（當時兩條設計斷言 FAIL）；長按改 ×2.5、人物貼底後升回 4150／87%，Combo 打法對搶高分的優勢回復，但**可及性約束的邊際價值被高速度稀釋**（AI 幾乎全屏可達，相關斷言門檻放寬，見 `catch_sim.py` 註解）。若要把難度拉回來，槓桿在生成間隔（少一點有價物）或炸彈比例，或縮短加速暖機。
- 掉落區 **6 條軌道各 80px**，同一軌道連續生成間隔不得低於 0.6 秒。
- 珠寶 50 / 星塵珍珠 100 / Charm 300（每 15 秒必出）/ 炸彈 −1 命 / 月光能量 **+150**（2026-09 護盾取消，純加分、吃 Combo 倍率）。
- Combo：每 5 連 +1 倍率上限 ×5，漏接有價物或吃到炸彈歸 1。
- 四段落速 60→80→100→120 px/s，每 15 秒一跳，炸彈率 6.5%→22.75%（原值的 0.65 倍，2026-09 掉落物翻倍時企劃拍板「炸彈增多 30%、不用翻倍」，見下）。
- 3 條命。**護盾系統已於 2026-09 整體移除**（原月光能量＝8 秒護盾、B/X 主動引爆清炸彈，全部拆除）：月光能量只是稀有加分物，每局最多出現 3 個、漏接照樣斷 Combo；接到炸彈一律扣命，**B／X 鍵自此沒有任何功能**。移除後模擬：中位分 4250→4500（月亮也吃倍率）、存活率 53%→42%、平均局長 53.1→51.3 秒 —— 沒有護盾兜底，炸彈實質變危險了。
- **生成間隔（GDD 沒寫，我們補的）**：四段各為 1.6~2.2 / 1.4~2.0 / 1.2~1.8 / 1.0~1.5 秒（2026-09 掉落物翻倍後減半；第 4 段又依企劃追加的末段加壓再 ÷1.3 成 0.385~0.575，見下條）。GDD 的「同屏上限」是**上限不是目標** —— 一路生到頂會變成每秒掉 2.6 顆、一局灑出 57 顆有價物，遠超過接得完的量，Combo 會整局卡在 ×1。
- **掉落物數量翻倍（2026-09 企劃）**：生成間隔全部減半（0.8~1.1 / 0.7~1.0 / 0.6~0.9 / 0.5~0.75 秒）、同屏上限 2/3/4/5→4/6/8/10；**炸彈比例設為原值的 0.65 倍**（10%→6.5% 一路到 35%→22.75%）＝ 企劃拍板「炸彈增多 30%、不用翻倍」。連炸彈一起翻倍時 AI 存活率 68%→19%、平均局長 56s→45s，局提前結束，總掉落數與分數反而縮水（`catch_sim.py` 第 6 節示範）；0.65 倍時炸彈 2.1→約 2.6 顆/局、死亡 1.7→約 2.1 次/局、存活率 67%→約 50%，有價物 28.6→約 59 顆/局（1.95 倍）。**末段加壓（企劃追加）**：第 4 段（45-60s）gap 再 ÷1.3（0.385~0.575）、炸彈比例不動 —— 有價物與炸彈共用同一條生成計時器，兩者剛好各 +30%（段內有價物 12.7→16.5 顆、炸彈生成 3.6→4.7 顆；整局存活率 50%→53%，末段落速 120px/s 多數炸彈落地被閃掉，難度漲幅溫和）。副作用：密度升高後「快落地」的那顆有價物幾乎總在低位，可及性約束幾乎不再縮窄候選，第 2 節斷言門檻從 +0.3 放寬到 +0.1、接取率門檻從 +10 放寬到 +5（跟當年速度 60→95 稀釋約束同一個現象）；接取率 87%→69%、最高倍率分佈下移（×4/×5 變少）—— 東西多了 Combo 本來就更容易斷。
- **有價物的生成軌道**必須落在「接完前一顆還追得上」的範圍內（炸彈不受限）。沒有這條的話 Combo 是擲骰子，不是走位。

---

## 待辦（依優先序，含理由與已知的坑）

### 1. 實機試玩定案 SUBTLE 或 ARCADE ｜ 需要人玩，模擬答不出來

三款目前都掛 ARCADE。改法是各遊戲的 `Juice.new(Juice.SUBTLE)` 換一個字。
試玩時特別注意：**擠壓幅度會不會太誇張**（目前最大形變 28%）、**最後 10 秒的暗角會不會擋到 Seeker 的珍珠**（珍珠只有 3px）。

### 2. 對齊 Seeker 與 GDD 的差異 ｜ 卡在企劃決定，見下方「待確認」

珍珠數與被抓到的處理都需要拍板才能動。

### 3. M5：每 20 秒加一隻暗影猫（上限 8），清空重鋪時額外加一隻

GDD 有明確規格。這是 Seeker 現在最缺的東西 —— **整局都是三隻貓，60 秒下來壓力曲線是平的**，後半段沒有升溫。

已知的坑（照做可以省很多時間）：

- `_spawn_cat(kind, home, delay)` 刻意設計成可以中途呼叫，但它**只建立節點**。`setup()` 是在 `_enter_ready()` 對整個 `cats` 陣列統一做的、`set_process(true)` 在 `_enter_playing()` —— 中途生的貓會少這兩步，要在新的 `_add_cat()` 裡自己補。
- **`cats` 陣列跨局存活**。`_start_round()` 必須把多出來的貓移除並歸位回 3 隻，否則重玩一局會愈玩愈多。這是最容易漏的一點。
- 需要 8 個出生格，目前只硬寫了 3 個。中央 y=6 與 y=7 在 x=11~16 都是通路，`seeker_sim.py` 可以驗。
- 新貓要給約 1 秒的 `release_delay`，沿用既有的半透明待命 —— 不然牠會無預警出現在中央、可能就貼在露娜臉上。
- **做完一定要重跑 `seeker_sim.py`**。5 隻貓的難度跟 3 隻差很多，基準是「會閃避的玩家平均每局死 0.63 次、30/30 撐完」。另外貓變多會讓單次石化的擊碎上限從 350 升到 1150（5 隻）甚至 2350（8 隻），單局分數上限會明顯上抬，重跑模擬確認中位數的變化。

### 4. M6：接美術素材、音效、Xbox 手把

交付順序見 `Guides/art-spec-charms-seeker.md` 第六節。
**音效是目前最大的缺口** —— 8-bit 遊戲的打擊感有一半以上來自聲音，
現在完全沒有。接上去的感受提升會比再加任何視覺特效都大。

### 5. M7：匯出 Windows / Web，接排行榜

---

## 待確認（GDD 與現況對不上，需要企劃拍板）

- **Seeker 珍珠數**：GDD 寫 180 顆，美術規格書寫「場上有 200 顆珍珠」，目前實作是 205 顆。
- **Seeker 被抓到的處理**：GDD 寫「碰撞後有 1.5 秒無敵時間」，目前實作是停頓 1.2 秒後把雙方位置重置。兩種手感差很多。

---

## 相關文件

- `Guides/CharmsSeeker.docx`、`CharmsFishing.docx`、`CharmsCatch.docx` — **三份 GDD，規格的最終權威**
- `Guides/art-spec-charms-seeker.md` — 美術素材規格書（尺寸、幀數、命名、交付順序）
- `Guides/charms-palette.png` — 鎖定的 20 色共用色盤，程式端對應 `shared/palette.gd`（程式端另有 2026-09 企劃指定的 MOON_LIGHT 與 ACCENT_* 三色，共 24 色）
- `Guides/godot-charms-seeker-guide.md` — M1 的建置紀錄（檔案路徑是重組前的舊版，結構以本檔為準）
- `tools/sim/README.md` — 平衡模擬腳本的用法與同步紀律

---

## 給協作者的建議

一次只做一個 Milestone。修改前先讀相關檔案，明確指出不要動哪些部分。
**每個 Milestone 完成就 commit**，改壞了直接 `git checkout .` 回退比慢慢修快得多。

在分支上開發，一個 Milestone 完成就開 PR 併回 `main`。歷史用 merge commit
保留（不 squash）—— 這個專案很多決定的理由寫在 commit message 裡，
squash 掉之後 `git bisect` 與 `git log` 就查不到「為什麼當初這樣改」。

動到任何影響玩法的數值時，先跑一次 `tools/sim/` 對應的腳本記下基準，
改完再跑一次比較 —— 這個專案的平衡全靠數字，用看的判斷不出來。
