# CharmsGames

Pandora 品牌合作的三款 8-bit 像素小遊戲，共用世界觀、色盤與結算系統。

**引擎**：Godot 4.7 / GDScript / Compatibility renderer
**目前進度**：三款都有可玩原型。Seeker 到 M4（月光能量石化 + 擊碎暗影猫），Fishing 與 Catch 依 GDD 完成首版。

**規格的權威來源是 `Guides/` 裡的三份 GDD**，不是這份檔案。本檔只記「實作上的決定」與「GDD 沒寫或寫不通、我們自己補的部分」。

---

## 硬規則（違反這些會造成實際問題）

- **邏輯畫面固定 480×270**，一格 tile 16px，4 倍整數放大顯示。所有座標都以這個為準，不要改。
- **不要修改 `.tscn` 場景檔**。整個專案只有一個場景 `Launcher.tscn`，裡面只有一個 Node2D。所有遊戲物件都用程式碼 `add_child()` 生成。這是刻意的架構決定 —— 讓所有邏輯都在純文字的 `.gd` 檔裡，方便 AI 讀寫與 git diff。
- **格子座標一律 `Vector2i`，像素座標一律 `Vector2`**。兩者用各遊戲自己的 `cell_center()` 轉換。混用是這個專案最容易出的 bug。
- **不要引入外部套件或 addon**。
- **不要用物理碰撞（CharacterBody2D 等）做格子移動**。已經用純數學判斷實作，改用物理會造成卡牆角與抖動。
- **HUD 文字一律用英文**。Godot 內建預設字型沒有中文字符，中文會變成方框。等像素中文字型接進來再改。
- **畫面位移（震動、鏡頭偏移）只能走繪製層，絕對不要寫進 `position`**。`player.gd` / `cat.gd` 每幀都用 `position` 算移動與 9px 碰撞判定，位移寫進去會被修正掉、過格時被清空，還會動到判定。用 `draw_set_transform()`，有子節點的就包一層容器節點（Seeker 的 `_world`）。

---

## 目錄結構

```
res://
├── Launcher.tscn           唯一的場景（Node2D + launcher.gd）
├── launcher/
│   └── launcher.gd         啟動選單，按 1/2/3 進遊戲、ESC 返回
├── games/
│   ├── seeker/             CharmsSeeker：迷宮追逐（小精靈）
│   │   ├── seeker.gd       主程式與狀態機
│   │   ├── maze.gd         迷宮資料（class_name Maze）
│   │   ├── player.gd       露娜的格子移動（class_name Player）
│   │   └── cat.gd          暗影猫追逐 AI（class_name Cat）
│   ├── fishing/
│   │   └── fishing.gd      CharmsFishing：黃金礦工式垂釣（單檔）
│   └── catch/
│       └── catch.gd        CharmsCatch：接珠寶躲炸彈（單檔）
├── shared/
│   ├── palette.gd          20 色共用色盤（class_name Palette）
│   └── juice.gd            震動／鏡頭偏移／視差／頓格（class_name Juice）
├── assets/                 美術素材（尚未進場，目前都是空資料夾）
│   ├── seeker/  fishing/  catch/
│   └── shared/             露娜、Charm、字型等三款共用
└── Guides/                 GDD、美術規格書、色盤
```

啟動選單用 `load(path)` + `set_script()` 把遊戲掛到臨時節點上，所以新增遊戲不需要新增場景，只要在 `launcher.gd` 的 `GAMES` 陣列加一筆。

生成順序不能調換：`load()` → `Node2D.new()` → `set_script()` → `add_child()`，因為 `add_child()` 那一刻才會觸發遊戲的 `_ready()`。回選單時 `remove_child()` + `queue_free()` 整個節點，遊戲自己生的子節點跟著消失，所以每款遊戲**不需要各自寫清理邏輯**。

`GAMES` 裡指到還不存在的腳本不會讓選單當掉，只會顯示 COMING SOON。

---

## Game feel（`shared/juice.gd`）

三款共用同一顆狀態機：撞擊震動、鏡頭偏移、背景視差、命中頓格。兩組預設 `SUBTLE` / `ARCADE`，在各遊戲的 `Juice.new(...)` 換一個字就切換，**還沒實機試玩定案**。

**分層模型**（三款一致，畫的順序就是這三層）：

| 層 | 內容 | 位移 |
|---|---|---|
| FAR | 星星、月亮、遠山／屋頂剪影 | `bg_offset()` |
| WORLD | 玩家、敵人、道具，**以及它們實際踩著的背景元素** | `world_offset()` |
| HUD | 分數、時間、生命、結算文字、全螢幕閃光 | 恆為 0 |

「實際踩著的」是關鍵：**水面線、水體、地面矩形、地面線都算 WORLD 不是背景**。船釘在水面線上、露娜站在地面線上 —— 讓接觸雙方共用同一個位移，脫節就變成結構上不可能。HUD 恆定不動，因為 8px 字在 4 倍放大下抖 1px 是螢幕上 4px 的跳動，看起來是 bug。

背景一律往外多畫 `Juice.OVERDRAW`（24px），震動時才不會露出沒人畫的邊。

**幾個踩過的坑**：
- 振幅用 `pow(trauma, 1.5)` 不是平方。平方在 `shake_max=6` 時會讓 trauma 0.2 只產生 0.24px、四捨五入後是 0，小碰撞完全看不見。平方原本的理由是「讓尾巴掉到門檻以下」，但 trauma 是線性衰減、本來就精準歸零，理由不成立。
- trauma **線性**衰減。指數衰減永遠到不了 0，殘留的次像素值四捨五入後會變成永久的 ±1px 抽動。
- 震動用固定 Hz 重新取樣，不是每幀重抽 —— 每幀重抽在 144Hz 螢幕上會變成高頻嗡嗡聲。
- 漂移用 `1-exp(-k*dt)`，固定係數的 lerp 收斂速度會隨 FPS 改變。
- 位移**整個合成值只四捨五入一次**，分別 round 再相加會讓兩層對不齊。
- 頓格期間整個 `_run_state` 跳過，所以 `time_left` 不會減 —— **玩家一秒都沒被偷走**（200 局模擬確認分數膨脹 0.00%）。
- Seeker 的星點用 `FAR` 不能用 `PEARL`：`PEARL` 就是星塵珍珠本身，會變成假目標。

---

## 三款共通的設計規格

- **單一關卡，60 秒**，時間到即結算，不做關卡遞進。
- **共用外框**：選單 → 遊戲 → 結算（分數 + 寶箱等級）→ 重來。
- **寶箱門檻**：Seeker/Catch 是 1500/3000/5000，Fishing 是 1000/2000/3500（銅/銀/金）。
- **狀態機模式統一**：`READY`（開場停頓 1.5 秒）→ 遊玩 → `RESULT`。暫停角色用 `set_process(false)`，不要在各處加 `if` 判斷。

---

## 各遊戲玩法規格

### CharmsSeeker（迷宮追逐）
- 28×14 tile 迷宮，外框牆 + 矩形障礙塊（`maze.gd` 的 `BLOCKS`）。每塊之間至少隔一格且不碰外框，走道即保證連通。
- 約 205 顆星塵珍珠（+10）、四角月光能量（+30）、清空全場 +500 並重鋪。
- 露娜 72 px/s，Pac-Man 式移動：輸入存成 `want`，抵達格子中心才轉彎；反方向可立即掉頭。
- 三隻暗影猫共用同一套貪婪移動：排除回頭路，選「走過去離目標最近」的方向，順序優先級 上→左→下→右。死路才准回頭。差別只在目標格算法與速度：
  - **直追** 60 px/s（`CAT` 身體主色）：目標＝露娜所在格。
  - **預判** 66 px/s（`CAT_GLOW` 最亮的紫）：目標＝露娜前方 4 格，會和直追貓自然形成夾擊。露娜靜止時退化成直追。
  - **遊蕩** 54 px/s（`CAT` 描邊 + `CAT_DARK` 填色）：目標＝隨機走道格，每 2.5 秒重抽；露娜進入 6 格內就改成直追。
  - 三隻只能用色盤上的三個暗影猫紫，所以辨識靠亮度與剪影而不是色相。玩家真正用來定位威脅的是那雙黃眼睛（`CAT_EYE`，全畫面唯一暖色）。
- 出生格在迷宮中央同一條走道（13,6）/（12,6）/（14,6），登場時間錯開 0 / 2.5 / 5 秒，未登場時畫成半透明。
- 3 條命，碰到扣 1 命（判定距離 9px），進入 `DYING` 1.2 秒後重生，**分數與剩餘時間保留**。
- **月光能量不是撿到就發動**（GDD 的 Xbox 協議）：撿到 +30 分並存進 HUD，最多囤 2 個，按 **A** 才啟動 8 秒石化。石化期間貓站著不動、畫成半透明藍白，撞上去就碎；剩 2 秒時貓閃白且畫面邊緣閃爍。
- **擊碎分數**同一次石化內倍增 50→100→200→400（第五隻之後維持 400）。被擊碎的貓 5 秒後於出生格重生。
- **重生一律回到 ACTIVE，不會以石化狀態回來**（GDD 沒寫，我們定的）：三隻出生格在中央緊鄰，若石化重生就會變成「中央蹲點等 5 秒再撞一輪」，一次石化可刷 1550 分（比銅寶箱還多）。回到 ACTIVE 後單次石化上限是 350。重生後有 1 秒半透明待命，不會無預警秒扣命。
- **撞牆會震一下**（`player.gd` 的 `bumped` signal，只在移動中被擋下時觸發一次）；按著方向鍵頂牆則是 `blocked` 旗標，改用鏡頭傾斜表現 —— 那個分支每幀都成立，發 signal 會變成每秒 60 次的閃頻。
- **Seeker 的移動只吃方向鍵，不再支援 WASD** —— A 鍵被 GDD 指定為主動技能，會跟 WASD 的 A 打架，而 GDD 本來就只寫方向鍵與左搖桿。

### CharmsFishing（黃金礦工）
- 鉤子在船底支點 (240, 96) 以 ±75° 擺動，週期 2.4 秒，**最後 15 秒擺速 +15%**。
- **A / 空白 / 方向鍵下**放線（GDD 指定 A），放出去不能取消。**B / X** 用月光能量，每局 3 次，只在收線途中有效：寶物 ×3 速，廢物直接丟棄止損。
- **分數**：雜魚 10 / 小珍珠 50 / 大魚 100 / 星塵珍珠 200 / Charm 500 / 石頭 0 / 暗影猫魚 −3 秒。
- **重量是另一回事**（舊版本檔把兩者混為一談）：重量只決定收線速度，輕→快、中→普通、重→慢、極重→極慢。石頭最重最慢，Charm 只是中等。
- 無生命值，錯誤只以「浪費時間」懲罰。
- **補充機制（GDD 沒寫，我們加的）**：星塵珍珠與 Charm 維持 GDD 的固定顆數、撈完就沒；雜魚／小珍珠／大魚／石頭／暗影猫魚撈走後會有新的游進來。不加這條的話盤面總值上限只有約 3070 分，金寶箱門檻 3500 在數學上拿不到。
- **水層不是等面積的**：鉤子可及範圍是倒三角形，淺層只有約 10000px²（放得下約 13 個），中／深層各約兩倍。淺層的物件配額要照這個算。放置時一律檢查「鉤得到」，避免生出永遠撈不到的死內容。

### CharmsCatch（接珠寶）
- 露娜在底部左右移動 **95 px/s**，**按住 A** 衝刺 ×1.8，**鬆開後 0.5 秒冷卻**。提籃頂面判定框 36×8，側邊擦過不算。
- **速度刻意偏離 GDD**（GDD 寫 60 px/s）：60 玩起來太鈍，一趟落下只夠橫移 1.4~2.9 軌，常常眼睜睜看珠寶掉在搆不到的地方。企劃試玩後決定調到 95 並加上慣性（`ACCEL` 900 / `FRICTION` 700，放開後滑約 0.14 秒 7px），起步跟手、收手會滑一小段。用純數學 `move_toward`，不引入物理節點。
- **這個改動讓難度明顯下降**（120 局模擬）：接取率 79%→86%，SILVER 67%→91%，**GOLD 13%→36%**。慣性本身幾乎不影響平衡（純手感），變化來自速度。若要把金寶箱拉回 10~15%，槓桿在生成間隔（少一點有價物）或炸彈比例，不要動共用的寶箱門檻。
- 掉落區 **6 條軌道各 80px**，同一軌道連續生成間隔不得低於 0.6 秒。
- 珠寶50/星塵珍珠100/Charm 300（每15秒必出）/炸彈-1命/月光能量護盾8秒。
- Combo：每 5 連 +1 倍率上限 ×5，漏接有價物或吃到炸彈歸 1。
- 四段落速 60→80→100→120 px/s，每 15 秒一跳，炸彈率 10%→35%。
- 3 條命，**B / X** 主動引爆護盾清掉全場炸彈。月光能量每局最多出現 3 個。
- **生成間隔（GDD 沒寫，我們補的）**：四段各為 1.6~2.2 / 1.4~2.0 / 1.2~1.8 / 1.0~1.5 秒。GDD 的「同屏上限」是上限不是目標，一路生到頂會變成每秒掉 2.6 顆、一局灑出 57 顆有價物，遠超過接得完的量，Combo 會整局卡在 ×1。
- **有價物的生成軌道**必須落在「接完前一顆還追得上」的範圍內（炸彈不受限）。沒有這條的話 Combo 是擲骰子，不是走位。

---

## 待辦（依優先序）

1. **實機試玩決定 SUBTLE 還是 ARCADE**，並回頭調 `shared/juice.gd` 的觸發值。務必走 Project Manager → Run（見已知問題）。
2. **對齊 Seeker 與 GDD 的差異**（見下方「待確認」）：珍珠 180 vs 205 顆、碰撞後 1.5 秒無敵 vs 現在的重置位置。
3. **M5** 每 20 秒加一隻貓（上限 8）、清空重鋪時額外加一隻 —— `seeker.gd` 的 `_spawn_cat(kind, home, delay)` 已經可以在遊戲中途呼叫。
4. **M6** 接入美術素材、音效、Xbox 手把（交付順序見美術規格書第六節）
5. **M7** 匯出 Windows / Web，接排行榜與 MysteryBox

---

## 已知問題

**從編輯器按 F5/F6 執行時，遊戲收不到鍵盤輸入。** 視窗焦點正常、`_process` 正常運作，但 `Input.is_key_pressed()` 永遠回傳 false，`_input` 也收不到任何 `InputEventKey`。已排除：輸入法、手把漂移、Game Embed Mode 設定。

**繞過方式**：從 Godot Project Manager 選中專案按 **Run** 執行（獨立行程），鍵盤即正常。開發時編輯器只用來寫程式碼。這是環境問題，不是程式碼問題，不要試圖用改程式碼來「修」它。

---

## 相關文件

- `Guides/CharmsSeeker.docx`、`CharmsFishing.docx`、`CharmsCatch.docx` — **三份 GDD，規格的權威來源**
- `Guides/art-spec-charms-seeker.md` — 美術素材規格書（尺寸、幀數、命名、交付順序）
- `Guides/charms-palette.png` — 鎖定的 20 色共用色盤，程式端對應 `shared/palette.gd`
- `Guides/godot-charms-seeker-guide.md` — M1 的建置紀錄（檔案路徑是重組前的舊版，結構以本檔為準）

### 待確認（GDD 與現況對不上，需要企劃決定）

- **Seeker 珍珠數**：GDD 寫 180 顆，美術規格書寫「場上有 200 顆珍珠」，目前實作是 205 顆。
- **Seeker 被抓到的處理**：GDD 寫「碰撞後有 1.5 秒無敵時間」，目前實作是停頓 1.2 秒後把雙方位置重置。兩種手感差很多。
- **Fishing 大魚尺寸**：GDD 寫 16×32，但橫向游動的魚應該寬大於高，目前實作採 32×16。
- **Fishing 金寶箱 3500**：靠魚群補充機制才拿得到（模擬 15%）。若不要補充機制，門檻要降到 2600 左右。
- **Catch 金寶箱 5000**：目前模擬約 11% 可達，需要疊到 ×3 以上倍率。

---

## 給 AI 協作者的建議

一次只做一個 Milestone。修改前先讀相關檔案，明確指出不要動哪些部分。每個 Milestone 完成就 commit，改壞了直接 `git checkout .` 回退比讓 AI 自己修快得多。
